[user]
	name = Charles de Freitas
	email = me@chasbob.dev
	signingKey = 4166401A

[core]
	pager = delta --plus-color="#012800" --minus-color="#340001" --theme='Monokai Extended'
	sshCommand = SSH_AUTH_SOCK=$(gpgconf --list-dirs agent-ssh-socket) ssh
	excludesfile = ~/.gitignore

[interactive]
	diffFilter = delta --color-only
[commit]
	gpgsign = true
[pull]
	ff = only
